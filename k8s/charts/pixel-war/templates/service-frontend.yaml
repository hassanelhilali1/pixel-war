apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    {{- include "pixel-war.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port:       {{ .Values.frontend.service.port }}
      targetPort: http
      protocol:   TCP
      name:       http
  selector:
    {{- include "pixel-war.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
